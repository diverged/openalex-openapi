get:
  summary: Get a random Concept
  description: Returns a random concept.
  responses:
    '200':
      description: Successful response
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/concept.yaml'
    '403':
      description: Forbidden
    '429':
      description: Too many requests
