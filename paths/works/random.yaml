get:
  operationId: randomWork
  summary: Get a random Work
  description: Returns a random work.
  responses:
    '200':
      description: Successful response
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/work.yaml'
    '403':
      description: Forbidden
    '429':
      description: Too many requests
