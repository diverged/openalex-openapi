get:
  summary: Get a random Author
  description: Returns a random author.
  responses:
    '200':
      description: Successful response
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/author.yaml'
    '403':
      description: Forbidden
    '429':
      description: Too many requests
