type: object
description: Concepts that are similar to this one. Each listed concept is a dehydrated Concept object, with one additional attribute - score (Float)
allOf:
  - $ref: './dehydrated_concept.yaml'
  - type: object
    properties:
      score:
        type: 
          - number
          - "null"
        description: The strength of association between this concept and the listed concept, on a scale of 0-100.
        example: 4.56749
