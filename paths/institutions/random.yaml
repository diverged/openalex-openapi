get:
  summary: Get a random Institution
  description: Returns a random institution.
  responses:
    '200':
      description: Successful response
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/institution.yaml'
    '403':
      description: Forbidden
    '429':
      description: Too many requests
