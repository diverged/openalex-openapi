get:
  operationId: randomSource
  summary: Get a random Source
  description: Returns a random source.
  responses:
    '200':
      description: Successful response
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/source.yaml'
    '403':
      description: Forbidden
    '429':
      description: Too many requests
